language: python
python:
    - '2.7'

cache: pip
#command to install dependencies
install:
  - pip install -r requirements.txt
  - pip install pytest pytest-cov
  - pip install coveralls pep8
  - pip install .

#command to run tests
script:
  - py.test --cov=pydefm tests/

after_success:
  - coveralls
